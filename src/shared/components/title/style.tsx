import styled from "styled-components"

export const Title = styled.h1`
    margin-bottom: 12px;
    text-align: left;
    color: ${({ theme }) => theme.defaultTextColor};
    font-weight: 400;
    font-size: 1.2rem;
`
